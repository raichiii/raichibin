alias res="exec $SHELL -l"
alias ls="ls --color=auto"
alias ll="ls -la"
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias grep="grep -iEI --color=auto"
alias screen="screen -c $RAICHIBIN_PATH/.screenrc"
alias scs="screen -S"
alias scr="screen -r"
alias scls="screen -ls"
alias gs="git status"
alias gitc="git commit -m"
alias ma="mamba activate"
alias mi="mamba install -y"
alias python="python -u"
alias py=python
alias lm="line-msg"
alias sleepon="sudo pmset -a disablesleep 0"
alias sleepoff="sudo pmset -a disablesleep 1"
alias hold="scontrol hold"
alias rel="scontrol release"
alias upd="scontrol update"
alias sq="squeue -o \"%.10i %.10P %.10j %.10u %.2t %.10M %.5D %.20R %.10q %.10Q %.10p\""

ac() {
    path=${$1:-.}
    . $1/.venv/bin/activate
}

ojpr() {
    num=$1
    ma ac
    mkdir -p /project/nblab/kkawahata/atCoder/abc${num}
    cd /project/nblab/kkawahata/atCoder/abc${num}
    oj-prepare https://atcoder.jp/contests/abc${num}
}

ojc() {
    t=${1:-2}
    name=${2:-main}
    others=${@:3}
    gcc $name.cpp -std=c++17 -lstdc++ -lm -I /project/nblab/kkawahata/atCoder/ac-library/ -fconcepts && \
    oj t -t $t -N ${others} && \
    oj s $name.cpp
}
ojp() {
    t=${1:-2}
    name=${2:-main}
    others=${@:3}
    ma ac
    oj t -c "python $name.py" -t $t -N ${others} && \
    oj s $name.py --guess-python-interpreter pypy
}
